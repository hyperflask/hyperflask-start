[project]
name = "{{cookiecutter.project_slug}}"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "hyperflask >=0.2"
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
    "pytest-cov>=6.2.1",
    "pytest-flask>=1.3.0",
    "ruff>=0.12.4",
    #"docker-web-deploy >=0.1"
]
